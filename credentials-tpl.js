// credentials-tpl.js

auth = {
    "ids": [ // ids (MAC addresses without colons) which are allowed to connect
        "************", // e.g. "12ab34cd56ef"
        "************"
    ]
}

module.exports = auth